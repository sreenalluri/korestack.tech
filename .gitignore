# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp

# Claude Code workspace
.claude/

# Lead-gen pipeline lives in its own (private) repo — never publish it here.
# It contains API keys (.env) and scraped prospect data (data/).
korestack-pipeline/
